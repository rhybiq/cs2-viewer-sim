# Optional: speech-to-text for the AI Viewer's clip transcript
# Transcribes spoken commentary/audio so the local VLM/LLM judges a clip
# based on what's said, not just what's shown.
# Not required for the base tool -- CPU-only by design (device="cpu" in
# transcribe_audio()), to avoid VRAM contention with Ollama's own vision
# model during the same transcription pass, not for lack of GPU support.
faster-whisper>=1.0
